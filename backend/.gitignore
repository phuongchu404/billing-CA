# Project exclude paths
/target/
/thirdparty-api/target/
/subscription-provider/target/
/plan-mng-web/target/
/subscription-api/target/

# RSA key files — do not commit private keys
**/keys/*.pem

# Docker env — chứa secrets, không commit
.env
